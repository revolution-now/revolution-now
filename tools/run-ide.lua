-----------------------------------------------------------------
-- Aliases
-----------------------------------------------------------------
local format = string.format

-----------------------------------------------------------------
-- Open IDE.
-----------------------------------------------------------------
local mode = ...
if not mode then mode = 'rn' end

require( format( 'ide.edit-%s', mode ) )
