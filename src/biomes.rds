# ===============================================================
# biomes.rds
#
# Project: Revolution Now
#
# Created by David P. Sicilia on 2026-03-29.
#
# Description: Rds definitions for the biomes module.
#
# ===============================================================
# ss
include "ss/terrain-enums.rds.hpp"

# refl
include "refl/enum-map.hpp"

namespace "rn"

struct.WetnessAdjustmentResult {
  iters 'int',
  max_allowed_iters 'int',
}

struct.AdjacencyAdjustmentResult {
  general_adjacency_iters 'int',
  swamp_marsh_adjacency_iters 'int',
  max_allowed_iters 'int',
  xs_regen 'double',
  general_adjacency_results [[
    refl::enum_map<e_biome, double>
  ]],
  general_adjacency_ratios [[
    refl::enum_map<e_biome, double>
  ]],
}
