# ===============================================================
# cartesian.rds
#
# Project: Revolution Now
#
# Created by David P. Sicilia on 2024-12-04.
#
# Description: Rds definitions for the gfx/cartesian module.
#
# ===============================================================
# gfx
include "compatibility.hpp" # FIXME: temporary

namespace "gfx"

enum.e_side {
   left, right,
}

enum.e_direction {
  nw, n, ne,
   w,     e,
  sw, s, se,
}

enum.e_cdirection {
  nw, n, ne,
   w, c,  e,
  sw, s, se,
}

enum.e_cardinal_direction {
      n,
   w,     e,
      s,
}

enum.e_diagonal_direction {
  nw,    ne,

  sw,    se,
}

enum.e_direction_type {
  cardinal, # n, w, e, s
  diagonal, # nw, ne, sw, se
}
